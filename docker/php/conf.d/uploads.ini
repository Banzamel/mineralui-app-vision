; Default PHP limits (2M / 8M) are too low for camera photos and main-photo uploads.
; Bumped to fit a single ~10 MP JPEG plus headroom for multipart overhead.
upload_max_filesize = 32M
post_max_size = 40M
memory_limit = 256M
max_file_uploads = 20
